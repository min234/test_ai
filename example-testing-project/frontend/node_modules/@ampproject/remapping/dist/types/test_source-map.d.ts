```python
import pytest
from your_module_name import SourceMap  # Replace 'your_module_name' with the actual module name
from unittest.mock import Mock

def test_source_map_constructor():
    mock_map = Mock()
    mock_options = Mock()
    source_map = SourceMap(mock_map, mock_options)
    assert isinstance(source_map, SourceMap)

def test_source_map_to_string():
    mock_map = Mock()
    mock_options = Mock()
    source_map = SourceMap(mock_map, mock_options)
    result = source_map.toString()
    assert isinstance(result, str)
```

- Replace `'your_module_name'` with the actual name of the module that contains the `SourceMap` class.
- This test file includes basic tests for the constructor and the `toString` method of the `SourceMap` class.
- Ensure you have `pytest` installed in your environment to run these tests.