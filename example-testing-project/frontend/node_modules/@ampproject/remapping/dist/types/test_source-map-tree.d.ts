```python
import pytest
from your_module import MapSource, TraceMap, Sources, MapSource as MapSourceType

def test_map_source_initialization():
    fake_trace_map = TraceMap()
    fake_sources = []
    map_source = MapSource(fake_trace_map, fake_sources)
    assert isinstance(map_source, MapSourceType)
    assert map_source.map == fake_trace_map
    assert map_source.sources == fake_sources

def test_map_source_content():
    fake_trace_map = TraceMap()
    fake_sources = []
    map_source = MapSource(fake_trace_map, fake_sources)
    assert map_source.content is None

def test_map_source_ignore():
    fake_trace_map = TraceMap()
    fake_sources = []
    map_source = MapSource(fake_trace_map, fake_sources)
    assert not map_source.ignore
```