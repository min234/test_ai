```python
import pytest

def test_segment_object():
    result = SegmentObject('', -1, -1, '', None, False)
    expected = {'source': '', 'line': -1, 'column': -1, 'name': '', 'content': None, 'ignore': False}
    assert result == expected

def test_source():
    result = Source('map', ['src1', 'src2'], 'source', 'content', True)
    expected = {
        'map': 'map',
        'sources': ['src1', 'src2'],
        'source': 'source',
        'content': 'content',
        'ignore': True,
    }
    assert result == expected
```